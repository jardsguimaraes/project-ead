spring:
    application:
        name: ead-notification-service
    config:
        import: "configserver:"
        #optional: caso queira utilizar uma rota alternativa em caso de falha
        #com o config-server, as configurações devem estar neste yaml
        # import: "optional:configserver:"
    cloud:
        config:
            discovery:
                service-id: ead-config-server