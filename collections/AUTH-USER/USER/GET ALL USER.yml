info:
  name: GET ALL USER
  type: http
  seq: 1

http:
  method: GET
  url: "{{localhost-gateway}}/ead-authuser/users"
  headers:
    - name: Authorization
      value: Bearer {{serv_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
