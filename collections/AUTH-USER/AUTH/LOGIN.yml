info:
  name: LOGIN
  type: http
  seq: 1

http:
  method: POST
  url: "{{localhost-gateway}}/ead-authuser/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "Wilson",
        "password": "F176!=8#3y67"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
