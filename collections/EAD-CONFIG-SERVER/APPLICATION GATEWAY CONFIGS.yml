info:
  name: APPLICATION GATEWAY CONFIGS
  type: http

http:
  method: GET
  url: "{{localhost-config-server}}/ead-api-gateway/main"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
