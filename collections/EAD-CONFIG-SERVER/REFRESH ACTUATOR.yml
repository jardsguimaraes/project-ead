info:
  name: REFRESH ACTUATOR
  type: http

http:
  method: POST
  url: "{{localhost-gateway}}/ead-authuser/actuator/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
