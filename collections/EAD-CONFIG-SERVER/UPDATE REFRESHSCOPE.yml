info:
  name: UPDATE REFRESHSCOPE
  type: http

http:
  method: GET
  url: "{{localhost-gateway}}/ead-authuser/refreshscope"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
