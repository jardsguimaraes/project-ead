info:
  name: APPLICATION PAYMENT CONFIGS
  type: http

http:
  method: GET
  url: "{{localhost-config-server}}/ead-payment-service/main"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
