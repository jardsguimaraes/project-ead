info:
  name: APPLICATION AUTHUSER CONFIGS
  type: http
  seq: 1

http:
  method: GET
  url: "{{localhost-config-server}}/ead-authuser-service/main"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
