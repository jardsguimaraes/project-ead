info:
  name: LOGIN
  type: http
  seq: 2

http:
  method: post
  url: "{{localhost-gateway}}/ead-authuser/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "Wilson",
        "password": "F176!=8#3y67"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        const jsonResponse = res.getBody();

        bru.setGlobalEnvVar("serv_token", jsonResponse.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​